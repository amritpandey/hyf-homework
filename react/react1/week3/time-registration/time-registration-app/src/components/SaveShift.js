export const SaveShift = ({ saveShiftList }) => {
    return (
        <div>
            <button type="submit" onClick={saveShiftList}>
                Save Shift
            </button>
        </div>
    );
};
